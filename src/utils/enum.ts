export enum GameState {
  WAITING = "Waiting",
  RUNNING = "Running",
  PAUSED = "Paused",
  END = "End",
}

export enum Movement {
  LEFT = "Left",
  RIGHT = "Right",
  STATIONERY = "Stationery",
}

export enum PlatformType {
  NORMAL = "Normal",
  MOVING = "Moving",
  GROUND = "Ground",
}
